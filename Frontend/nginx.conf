server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # SPA: moi duong dan khong phai file that deu tra ve index.html, de
    # React Router tu xu ly. Thieu dong nay thi F5 o /admin/users se ra 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
